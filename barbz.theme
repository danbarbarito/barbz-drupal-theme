<?php

  function barbz_preprocess(&$variables) {
    $variables['site_base_url'] = \Drupal::request()->getHost();
    $variables['site_name'] = \Drupal::config('system.site')->get('name');
    $variables['site_slogan'] = \Drupal::config('system.site')->get('slogan');
  }

?>
